The Later app is React app used to determine when an event will happen. The user first chooses an origin date and time using a standard date and time picker. Then they add an arbitrary number of offsets. Each offset is configured with two inputs: a number picker and a unit dropdown. The options for the unit include seconds, minutes, hours, days, and years. The page displays the resulting later time.
